export const CUSTOM_TAG_STYLE = 'border-slate-300 bg-slate-100 text-slate-900 dark:border-slate-700 dark:bg-slate-800 dark:text-slate-100'

/* Kept for type safety in other files if needed, but we'll use string mostly */
export type NoteTag = string 
